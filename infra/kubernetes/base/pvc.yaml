apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tasklattice-control-data
  namespace: tasklattice-sandboxes
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
